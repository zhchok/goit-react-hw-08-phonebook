import { HiOutlineUserCircle } from "react-icons/hi";
import styled from "styled-components";

export const Avatar = styled(HiOutlineUserCircle)`
	margin-right: 4px;
`;

export const Name = styled.span`
	font-weight: 700;
	margin-right: 12px;
`;
